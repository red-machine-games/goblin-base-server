await lock.self();

var validation = await validateStoreReceipt({ receiptA: selfHumanId });

setProfileNode('profileData.tin', 'frighten');
setSelfRating('deposit', 322);

FunctionResponse(validation);