await lock.some([1, 2]);

var validation1 = await validateStoreReceipt({ receipt: 'A' }),
    validation2 = await validateStoreReceipt({ receipt: 'B' });

appendBattleJournalPve(1, { tract: 'compose' });
appendBattleJournalPve(1, { banner: 'monopoly' });

appendBattleJournalPve(2, { rubbish: 'cave' });
appendBattleJournalPve(2, { example: 'unfortunate' });

FunctionResponse({ okay: true });